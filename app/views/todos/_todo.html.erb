<tr>
  <td><%= todo.title %></td>
  <td><%= distance_of_time_in_words(Time.now, todo.created_at + 7.days) %></td>
  <td><%= link_to "", [@list, todo], method: :delete, class: 'glyphicon glyphicon-ok' %></td>
</tr>
